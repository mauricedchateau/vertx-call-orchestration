package nl.dechateau.vertx.orchestration;

/**
 * Marker interface for <code>handler</code>s within the orchestration that make calls over the event bus.
 */
public interface CallHandler extends Handler {
}
